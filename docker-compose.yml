services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-checkin}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-checkin}
      POSTGRES_DB: ${POSTGRES_DB:-checkin}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - checkin_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U ${POSTGRES_USER:-checkin} -d ${POSTGRES_DB:-checkin}",
        ]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  checkin_postgres_data:
